[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes so React Router works
# on direct navigation / refresh of any path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
